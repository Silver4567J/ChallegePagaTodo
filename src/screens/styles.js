import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: 'red'
    },
    boxText: {
        paddingHorizontal: 20
    },
    textNoInfo: {
        textAlign: 'center',
        fontSize: 20,
    }
})